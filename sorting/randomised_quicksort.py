
#This subroutine is used if we randomise the pivot.
# pivot_index = random.randrange(low,high)	
# pivot = array[pivot_index]

# if pivot_index != low:
# 	low_val = array[low]
# 	array[pivot_index] = low_val
# 	array[low] = pivot


